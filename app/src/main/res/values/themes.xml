<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- app theme used by MainActivity -->
    <style name="Theme.HuntQuest" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!-- splash-specific theme -->
    <style name="Theme.HuntQuest.Splash" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>